# ============================================================
# bocik — Minimal Docker runtime dependencies
# Stripped: no numba (150MB), no matplotlib (50MB), no rich/tqdm
# ============================================================

# Exchange API
ccxt>=4.4.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# Technical Analysis
ta-lib>=0.4.28

# ML inference (MetaLabeler)
scikit-learn>=1.5.0
joblib>=1.4.0

# Database
sqlalchemy>=2.0.30

# WebSocket
websocket-client>=1.8.0

# Configuration
pyyaml>=6.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# Chart generation (Pillow fallback, no matplotlib needed)
Pillow>=10.0.0

# Telegram alerts
python-telegram-bot>=21.3.0

# Logging
loguru>=0.7.2

# Utilities
tenacity>=8.3.0
schedule>=1.2.0
